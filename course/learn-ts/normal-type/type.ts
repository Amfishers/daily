const a:number = 1
const b : string = '1'
const c : boolean = true

// 下面两个都是表示一个意思
const d : Array<number> = [1,2,3]
const e : string[] = ['12', '2']

// 函数参数以及返回值类型定义
function getFavoriteNumber (num: number) : number {
    return 1 + num
}


